<%= link_to 'Nueva venta', new_sale_path, :class => 'menu_link' %>
<% if can? :see,'hq_links'%>
<% Headquarter.all.each do |hq|%>
<%= link_to hq.name, "#{sales_path}/sede/#{hq.name}", :class => 'menu_link_hq' %>
<% end%>
<% end%>