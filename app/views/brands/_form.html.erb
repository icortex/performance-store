<%= form_for(@brand) do |f| %>
    <%= render 'layouts/error_messages', :model => @brand %>

    <div class="field">
      <%= f.label 'Marca' %>
      <br/>
      <%= f.text_field :brand %>
    </div>
    <div align="center">
      <%= f.submit 'Aceptar' %>
    </div>
<% end %>
