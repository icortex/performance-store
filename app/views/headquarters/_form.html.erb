<%= form_for(@headquarter) do |f| %>
    <% if @headquarter.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@headquarter.errors.count, "error") %> prohibited this headquarter from being saved:</h2>

          <ul>
            <% @headquarter.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label 'Nombre' %>
      <br/>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label 'Direccion' %>
      <br/>
      <%= f.text_field :address %>
    </div>
    <div class="field">
      <%= f.label 'Telefono' %>
      <br/>
      <%= f.text_field :phone %>
    </div>
    <div class="field">
      <%= f.label 'Decripcion' %>
      <br/>
      <%= f.text_field :description %>
    </div>
    <div align="center">
      <%= f.submit 'Aceptar' %>
    </div>
<% end %>
